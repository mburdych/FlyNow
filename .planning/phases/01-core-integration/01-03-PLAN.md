---
phase: 01-core-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - custom_components/flynow/__init__.py
  - custom_components/flynow/coordinator.py
  - custom_components/flynow/sensor.py
  - tests/test_coordinator.py
  - tests/test_sensor.py
autonomous: true
requirements:
  - HA-02
  - HA-03
requirements_addressed:
  - HA-02
  - HA-03
must_haves:
  truths:
    - "HA exposes one binary sensor with GO/NO-GO state and all required multi-window attributes."
    - "Coordinator refreshes forecast on configured 30-60 minute cadence and supports manual refresh."
    - "Sensor state updates reflect current active window and per-window condition data from coordinator output."
  artifacts:
    - path: custom_components/flynow/coordinator.py
      provides: "DataUpdateCoordinator orchestration of fetch, windows, and analyzer"
    - path: custom_components/flynow/sensor.py
      provides: "binary_sensor.flynow_status projection with D-06/D-07 attributes"
    - path: custom_components/flynow/__init__.py
      provides: "entry setup/unload lifecycle and coordinator storage"
  key_links:
    - from: custom_components/flynow/coordinator.py
      to: custom_components/flynow/open_meteo.py
      via: "periodic async update call"
      pattern: "coordinator _async_update_data invokes Open-Meteo client"
    - from: custom_components/flynow/sensor.py
      to: custom_components/flynow/coordinator.py
      via: "CoordinatorEntity data projection"
      pattern: "is_on and attributes read only from coordinator.data"
---

<objective>
Wire the integration runtime so HA receives a production-ready `binary_sensor.flynow_status` updated by a DataUpdateCoordinator using the tested weather/time analysis services.

Purpose: Complete HA-facing behavior for sensor state and refresh lifecycle.
Output: coordinator lifecycle, entity projection, and integration tests validating HA-02 and HA-03.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-core-integration/01-CONTEXT.md
@custom_components/flynow/const.py
@custom_components/flynow/open_meteo.py
@custom_components/flynow/windows.py
@custom_components/flynow/analyzer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement coordinator runtime and config-entry lifecycle</name>
  <read_first>
    - .planning/phases/01-core-integration/01-RESEARCH.md
    - custom_components/flynow/open_meteo.py
    - custom_components/flynow/windows.py
    - custom_components/flynow/analyzer.py
  </read_first>
  <files>custom_components/flynow/coordinator.py, custom_components/flynow/__init__.py, tests/test_coordinator.py</files>
  <action>Implement `FlyNowCoordinator(DataUpdateCoordinator)` with update interval from config entry and first refresh during `async_setup_entry`. In `_async_update_data`, call Open-Meteo client (D-15), build windows (D-11/D-12/D-13), analyze each window (D-01/D-02/D-03/D-04), and store normalized coordinator data contract for the sensor. Implement unload forwarding and cleanup in `__init__.py`. Add tests for periodic refresh interval behavior (HA-03), first refresh success path, and graceful handling of upstream failures.</action>
  <verify>
    <automated>python -m pytest tests/test_coordinator.py -q</automated>
  </verify>
  <acceptance_criteria>Coordinator lifecycle is HA-compliant, refresh cadence follows user setting (30-60 min bounds), and data contract is available after first refresh.</acceptance_criteria>
  <done>Coordinator and setup/unload lifecycle operate without blocking I/O in entity layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement binary sensor projection with full multi-window attributes</name>
  <read_first>
    - .planning/phases/01-core-integration/01-CONTEXT.md
    - custom_components/flynow/coordinator.py
  </read_first>
  <files>custom_components/flynow/sensor.py, tests/test_sensor.py</files>
  <action>Create one `binary_sensor.flynow_status` entity per D-05. Map `is_on` to active decision and publish D-06 attribute contract: `active_window`, `launch_start`, `launch_end`, and all evening/morning window condition blocks with D-07 fields/threshold echoes. Keep entity properties memory-only (no network calls or computation-heavy logic). Add tests asserting exact attribute keys, active-window semantics, and missing/expired window handling.</action>
  <verify>
    <automated>python -m pytest tests/test_sensor.py -q</automated>
  </verify>
  <acceptance_criteria>Sensor exposes one authoritative GO/NO-GO state with complete per-window attributes needed by Phase 2 card/automation consumers.</acceptance_criteria>
  <done>Entity behavior is deterministic and derived solely from coordinator data.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration-level verification for manual refresh and end-to-end state update</name>
  <read_first>
    - custom_components/flynow/__init__.py
    - custom_components/flynow/coordinator.py
    - custom_components/flynow/sensor.py
  </read_first>
  <files>tests/test_coordinator.py, tests/test_sensor.py</files>
  <action>Add end-to-end tests that set up config entry, force coordinator refresh, and verify sensor state/attributes update within one cycle. Include manual refresh trigger path (`homeassistant.update_entity`) to satisfy roadmap refresh behavior and ensure state changes propagate from coordinator to sensor without restart.</action>
  <verify>
    <automated>python -m pytest tests/test_coordinator.py::test_manual_refresh_updates_sensor tests/test_sensor.py::test_sensor_updates_after_coordinator_refresh -q</automated>
  </verify>
  <acceptance_criteria>Manual refresh updates `binary_sensor.flynow_status` and attribute payload from latest coordinator data in the same test run.</acceptance_criteria>
  <done>HA-02 and HA-03 are validated by integration-style automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coordinator data -> sensor entity | Computed state crosses into HA-visible entity API |
| HA service call -> manual refresh path | External automation/user action triggers runtime update |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Denial of Service | coordinator update cycle | mitigate | Use DataUpdateCoordinator-managed interval and bounded processing per refresh cycle. |
| T-01-08 | Information Disclosure | sensor attributes | accept | Exposed data is non-sensitive weather/decision metadata; no secrets or PII included. |
| T-01-09 | Tampering | sensor state derivation | mitigate | Derive entity state exclusively from validated coordinator contract and test exact attribute mapping. |
</threat_model>

<verification>
Run `python -m pytest tests/test_coordinator.py tests/test_sensor.py -q` and verify all tests pass.
</verification>

<success_criteria>
- One binary sensor exposes full D-06/D-07 attribute schema and GO/NO-GO state.
- DataUpdateCoordinator refreshes on configured interval and supports manual refresh updates.
- Setup/unload lifecycle is async-safe and compatible with HA config-entry patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-integration/01-03-SUMMARY.md`.
</output>
