---
phase: 01-core-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/flynow/manifest.json
  - custom_components/flynow/const.py
  - custom_components/flynow/config_flow.py
  - custom_components/flynow/strings.json
  - tests/test_config_flow.py
autonomous: true
requirements:
  - HA-01
  - WEATHER-04
requirements_addressed:
  - HA-01
  - WEATHER-04
must_haves:
  truths:
    - "User can complete a 3-step config flow for one launch site without entering notification contacts."
    - "Configured thresholds are persisted and used as integration settings."
    - "Config values are validated with explicit bounds before entry creation."
  artifacts:
    - path: custom_components/flynow/config_flow.py
      provides: "3-step ConfigFlow implementation with schema validation and final entry creation"
    - path: custom_components/flynow/const.py
      provides: "Domain constants, config keys, defaults, Slovak day labels, threshold bounds"
    - path: tests/test_config_flow.py
      provides: "Automated tests for successful flow and validation errors"
  key_links:
    - from: custom_components/flynow/config_flow.py
      to: custom_components/flynow/const.py
      via: "shared config keys/defaults/validation limits"
      pattern: "CONF_.* and DEFAULT_.* imports"
    - from: custom_components/flynow/config_flow.py
      to: custom_components/flynow/strings.json
      via: "matching step/field keys"
      pattern: "step IDs and field names are identical"
---

<objective>
Deliver the Home Assistant integration scaffold and locked 3-step setup flow so FlyNow can store one-site location, flight parameters, and user-configurable thresholds.

Purpose: Establish the required HA config-entry contract and enforce Phase 1 boundary decisions early (D-08, D-09, D-10).
Output: Working manifest/constants/config flow/strings files with tests validating setup and input constraints.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-integration/01-CONTEXT.md
@.planning/phases/01-core-integration/01-RESEARCH.md
@.planning/phases/01-core-integration/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration manifest and configuration constants</name>
  <read_first>
    - .planning/phases/01-core-integration/01-CONTEXT.md
    - .planning/phases/01-core-integration/01-RESEARCH.md
  </read_first>
  <files>custom_components/flynow/manifest.json, custom_components/flynow/const.py</files>
  <action>Create `manifest.json` with `domain=flynow`, `config_flow=true`, and HA-compatible metadata. Create `const.py` with domain constant, all config keys/defaults from D-09, single-site semantics per D-10, and threshold/time validation bounds used by config flow and coordinator. Keep all wind defaults and keys in m/s per D-03 and WEATHER-04. Do not add any notification/contact fields in constants (deferred by D-08).</action>
  <verify>
    <automated>python -m pytest tests/test_config_flow.py::test_manifest_and_constants_contract -q</automated>
  </verify>
  <acceptance_criteria>`manifest.json` enables config flow and constants expose every required key/default for location, flight params, and thresholds with no contact-related fields.</acceptance_criteria>
  <done>Manifest and constants compile and are importable; all required keys used by later plans are present.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 3-step ConfigFlow with strict bounds and one-site policy</name>
  <read_first>
    - .planning/phases/01-core-integration/01-CONTEXT.md
    - custom_components/flynow/const.py
  </read_first>
  <files>custom_components/flynow/config_flow.py, custom_components/flynow/strings.json</files>
  <action>Implement exact 3-step flow from D-09: location -> flight parameters -> thresholds. Enforce one config entry for Phase 1 per D-10. Validate lat/lon ranges, interval/duration/prep ranges, and threshold ranges before advancing. Persist final combined entry data at flow completion. Add matching localization entries in `strings.json` for each step/field and validation error text. Keep flow scoped to HA-01 and WEATHER-04; do not introduce reconfigure for contacts or multi-site.</action>
  <verify>
    <automated>python -m pytest tests/test_config_flow.py::test_three_step_flow_happy_path tests/test_config_flow.py::test_flow_rejects_out_of_bounds_inputs -q</automated>
  </verify>
  <acceptance_criteria>User can complete all 3 steps, invalid values are rejected with deterministic errors, and entry data includes all fields required by D-09.</acceptance_criteria>
  <done>Config entry creation succeeds with valid input and fails safely for out-of-range values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add automated config-flow contract tests</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - custom_components/flynow/config_flow.py
  </read_first>
  <files>tests/test_config_flow.py</files>
  <action>Write pytest-homeassistant-custom-component tests proving HA-01 and WEATHER-04 behavior: exact 3-step order, one-site enforcement, mandatory fields, and threshold persistence. Include negative test asserting notification contact fields are absent in Phase 1 (D-08). Keep fixtures lightweight and deterministic.</action>
  <verify>
    <automated>python -m pytest tests/test_config_flow.py -q</automated>
  </verify>
  <acceptance_criteria>Tests fail when step order/bounds/scope are violated and pass when implementation matches D-08/D-09/D-10.</acceptance_criteria>
  <done>Config flow behavior is regression-protected by focused automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> config flow | Untrusted setup values cross into persisted configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `config_flow.py` user inputs | mitigate | Validate numeric ranges and types before storing config entry data; reject malformed values with explicit errors. |
| T-01-02 | Denial of Service | `config_flow.py` update interval field | mitigate | Enforce minimum/maximum interval bounds so users cannot set harmful polling rates. |
| T-01-03 | Elevation of Privilege | config entry multiplicity | accept | Single-site restriction (D-10) reduces complexity and exposure; no privileged actions are introduced in this plan. |
</threat_model>

<verification>
Run `python -m pytest tests/test_config_flow.py -q` and confirm all tests pass.
</verification>

<success_criteria>
- HA setup provides an exact 3-step flow with one-site policy and no contact fields.
- Threshold and timing values are persisted and validated.
- Automated tests cover success and failure paths for HA-01 and WEATHER-04.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-integration/01-01-SUMMARY.md`.
</output>
