---
phase: 01-core-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - custom_components/flynow/windows.py
  - custom_components/flynow/analyzer.py
  - custom_components/flynow/open_meteo.py
  - tests/test_windows.py
  - tests/test_analyzer.py
  - tests/test_open_meteo.py
autonomous: true
requirements:
  - WEATHER-01
  - WEATHER-02
  - WEATHER-03
  - WEATHER-05
  - TIME-01
  - TIME-02
  - TIME-03
  - TIME-04
  - FORE-01
  - FORE-02
requirements_addressed:
  - WEATHER-01
  - WEATHER-02
  - WEATHER-03
  - WEATHER-05
  - TIME-01
  - TIME-02
  - TIME-03
  - TIME-04
  - FORE-01
  - FORE-02
must_haves:
  truths:
    - "System can fetch all required Open-Meteo hourly and daily fields for one configured site."
    - "Both evening and morning launch windows are computed with sunrise/sunset constraints."
    - "Each window returns GO/NO-GO using strict AND and worst-case values across full flight duration."
    - "Look-ahead and night-before windows are present as explicit day+2/day+3 and tomorrow windows."
  artifacts:
    - path: custom_components/flynow/open_meteo.py
      provides: "Open-Meteo client and normalized forecast payload"
    - path: custom_components/flynow/windows.py
      provides: "7-window builder with skip logic and Slovak labels"
    - path: custom_components/flynow/analyzer.py
      provides: "Strict AND + worst-case window evaluator with condition breakdown"
  key_links:
    - from: custom_components/flynow/open_meteo.py
      to: custom_components/flynow/windows.py
      via: "normalized hourly/daily forecast structure"
      pattern: "daily.sunrise/sunset and hourly arrays consumed by window builder"
    - from: custom_components/flynow/windows.py
      to: custom_components/flynow/analyzer.py
      via: "window slices passed for threshold evaluation"
      pattern: "window start/end indexes mapped to per-window analysis input"
---

<objective>
Implement deterministic weather/time analysis services so coordinator integration can consume a tested contract for fetch, window construction, and GO/NO-GO evaluation.

Purpose: Satisfy all weather/time/look-ahead logic before wiring entities.
Output: Open-Meteo client, window builder, analyzer, and regression tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-integration/01-CONTEXT.md
@.planning/phases/01-core-integration/01-RESEARCH.md
@custom_components/flynow/const.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Open-Meteo client contract for required forecast fields</name>
  <read_first>
    - .planning/phases/01-core-integration/01-CONTEXT.md
    - .planning/phases/01-core-integration/01-RESEARCH.md
  </read_first>
  <files>custom_components/flynow/open_meteo.py, tests/test_open_meteo.py</files>
  <action>Create async `aiohttp` client helper that requests Open-Meteo only (D-15) with `timezone=Europe/Bratislava`, `forecast_days=7`, wind unit m/s (D-03), required hourly fields (surface + pressure levels + precip probability + visibility + ceiling when available), and daily sunrise/sunset (TIME-01). Normalize payload into typed dicts used by window/analyzer modules. Add failure handling that surfaces clear update errors for coordinator retries.</action>
  <verify>
    <automated>python -m pytest tests/test_open_meteo.py -q</automated>
  </verify>
  <acceptance_criteria>Client requests only Open-Meteo fields required by WEATHER-01/02/03 and TIME-01, normalizes shape predictably, and handles API errors without crashing caller.</acceptance_criteria>
  <done>Open-Meteo client is test-covered and ready for coordinator consumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build morning/evening window calculator with Slovak labels and skip rules</name>
  <read_first>
    - .planning/phases/01-core-integration/01-CONTEXT.md
    - custom_components/flynow/const.py
  </read_first>
  <files>custom_components/flynow/windows.py, tests/test_windows.py</files>
  <action>Implement pure functions for 7-window model per D-11 (today+3 evening, tomorrow+3 morning), evening/morning formulas per TIME-02/TIME-03, primary evening prioritization per TIME-04, and expired-window omission per D-12. Include Slovak day labels per D-13 and explicit flags for windows satisfying FORE-01 (day+2/day+3) and FORE-02 (tomorrow evening/morning).</action>
  <verify>
    <automated>python -m pytest tests/test_windows.py -q</automated>
  </verify>
  <acceptance_criteria>Window output includes correct bounds/labels, omits expired windows, and exposes tomorrow/day+2/day+3 coverage used by forecast requirements.</acceptance_criteria>
  <done>Deterministic window set is produced from sunrise/sunset and config parameters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement strict AND worst-case analyzer and per-window condition breakdown</name>
  <read_first>
    - .planning/phases/01-core-integration/01-CONTEXT.md
    - custom_components/flynow/windows.py
  </read_first>
  <files>custom_components/flynow/analyzer.py, tests/test_analyzer.py</files>
  <action>Implement evaluator using D-01 and D-02 exactly: compute worst-case values across every hour in each window (max surface/altitude wind, max precip probability, min ceiling, min visibility) and return GO only if all checks pass (strict AND). Use m/s throughout per D-03 and pressure-level proxies for altitude checks per D-04. Return per-window breakdown with metric values, `_ok` booleans, and threshold echoes aligned to D-07.</action>
  <verify>
    <automated>python -m pytest tests/test_analyzer.py -q</automated>
  </verify>
  <acceptance_criteria>Any single threshold breach produces NO-GO, full-window worst-case is used instead of launch-point values, and response structure contains all D-07 fields.</acceptance_criteria>
  <done>Analyzer produces finalized per-window decisions consumable by sensor attribute mapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Open-Meteo API -> integration runtime | Untrusted external payload enters domain calculations |
| Config thresholds -> analyzer | User-defined values influence safety decision logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | `open_meteo.py` response parsing | mitigate | Validate required keys/types and reject malformed payloads before window/analyzer execution. |
| T-01-05 | Denial of Service | window/analyzer loops | mitigate | Bound evaluation strictly to generated windows and hourly indices from forecast range. |
| T-01-06 | Repudiation | decision traceability | mitigate | Return metric values + thresholds + `_ok` flags for each window to make go/no-go decision auditable. |
</threat_model>

<verification>
Run `python -m pytest tests/test_open_meteo.py tests/test_windows.py tests/test_analyzer.py -q`.
</verification>

<success_criteria>
- Open-Meteo client fetches all required weather/time fields for 7-day forecast horizon.
- Window builder produces locked 7-window model and omits expired windows.
- Analyzer returns strict worst-case go/no-go with full condition breakdown for each window.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-integration/01-02-SUMMARY.md`.
</output>
