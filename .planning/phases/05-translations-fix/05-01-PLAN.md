---
phase: 05-translations-fix
plan: 01
type: execute
wave: 1
depends_on:
  - 04-multi-site-forecast-planning-card
files_modified:
  - custom_components/flynow/strings.json
  - custom_components/flynow/translations/sk.json
autonomous: true
requirements:
  - TR-01
  - TR-02
  - TR-03
  - TR-04
  - TR-05
must_haves:
  truths:
    - "strings.json contains `config.title` at the `config` level so HA displays 'FlyNow' in the config flow header."
    - "translations/sk.json mirrors the `config.title` key and all existing keys use correct Slovak diacritics."
    - "Both files remain valid JSON and structurally in sync (identical key sets)."
---

<objective>
Fix two gaps in the FlyNow translation layer: (1) missing `config.title` key that causes an empty header in the HA config flow UI, and (2) ASCII-only Slovak text in `translations/sk.json` that should use proper diacritics.

Purpose: Deliver a complete, well-formed translation baseline so the integration displays correctly in HA UI under both English and Slovak locales.
Output: Updated `strings.json` with `config.title`, and updated `translations/sk.json` with `config.title` and corrected Slovak diacritics.
</objective>

<execution_context>
@.planning/research/TRANSLATIONS-RESEARCH.md
</execution_context>

<verification>
- `python -c "import json; json.load(open('custom_components/flynow/strings.json', encoding='utf-8')); print('strings.json valid')"`
- `python -c "import json; json.load(open('custom_components/flynow/translations/sk.json', encoding='utf-8')); print('sk.json valid')"`
- `python -c "import json; s=json.load(open('custom_components/flynow/strings.json',encoding='utf-8')); sk=json.load(open('custom_components/flynow/translations/sk.json',encoding='utf-8')); assert 'title' in s['config']; assert 'title' in sk['config']; assert s['config']['title']=='FlyNow'; assert sk['config']['title']=='FlyNow'; print('OK')"`
</verification>

<success_criteria>
- `strings.json` contains `"title": "FlyNow"` at the `config` level.
- `translations/sk.json` contains `"title": "FlyNow"` at the `config` level.
- All Slovak text in `sk.json` uses correct diacritics (no ASCII substitutes remaining).
- Both files are valid UTF-8 JSON with identical key sets.
- HA config flow displays "FlyNow" in the header when adding the integration.
</success_criteria>
