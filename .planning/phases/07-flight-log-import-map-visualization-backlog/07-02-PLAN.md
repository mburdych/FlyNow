---
phase: 07-flight-log-import-map-visualization-backlog
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - custom_components/flynow/weather_snapshot.py
  - custom_components/flynow/coordinator.py
  - custom_components/flynow/flight_log.py
  - custom_components/flynow/sensor.py
  - tests/test_weather_snapshot.py
  - tests/test_coordinator.py
  - tests/test_flight_log.py
  - tests/test_sensor.py
autonomous: true
requirements:
  - BL-07-03
  - BL-07-04
  - BL-07-05
  - BL-07-10
must_haves:
  truths:
    - "Forecast context used for go/no-go can be frozen immutably at decision time."
    - "When snapshot is missing, finalize/import path backfills freeze behavior without data loss."
    - "Observed weather uses deterministic source priority: METAR, then archive API, then manual entry."
    - "Import succeeds even when weather lookup fails, with explicit missing-weather reason."
  artifacts:
    - path: custom_components/flynow/weather_snapshot.py
      provides: "Forecast freeze + observed weather provider-chain resolver"
    - path: custom_components/flynow/coordinator.py
      provides: "Decision-time snapshot capture hook"
    - path: custom_components/flynow/sensor.py
      provides: "Compact correlation projection for frontend consumption"
  key_links:
    - from: custom_components/flynow/coordinator.py
      to: custom_components/flynow/weather_snapshot.py
      via: "decision-time freeze invocation"
      pattern: "freeze|snapshot|decision"
    - from: custom_components/flynow/flight_log.py
      to: custom_components/flynow/weather_snapshot.py
      via: "import/finalize fallback snapshot capture"
      pattern: "finalize|import|weather_missing"
    - from: custom_components/flynow/sensor.py
      to: lovelace/flynow-card/src/types.ts
      via: "bounded correlation summary payload"
      pattern: "weather_snapshot|observed_source|weather_missing"
---

<objective>
Implement immutable forecast-vs-reality weather correlation so delayed imports remain reliable and auditable.

Purpose: Ensure Phase 07 can compare prediction and observed conditions even when records are imported days later.
Output: Snapshot resolver module, coordinator/finalize wiring, and compact sensor projection with test coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-flight-log-import-map-visualization-backlog/07-CONTEXT.md
@.planning/phases/07-flight-log-import-map-visualization-backlog/07-RESEARCH.md
@custom_components/flynow/coordinator.py
@custom_components/flynow/flight_log.py
@custom_components/flynow/sensor.py
@.planning/phases/07-flight-log-import-map-visualization-backlog/07-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement weather snapshot resolver with immutable records</name>
  <files>custom_components/flynow/weather_snapshot.py, tests/test_weather_snapshot.py</files>
  <read_first>
    - .planning/phases/07-flight-log-import-map-visualization-backlog/07-CONTEXT.md
    - .planning/phases/07-flight-log-import-map-visualization-backlog/07-RESEARCH.md
    - custom_components/flynow/coordinator.py
  </read_first>
  <action>Create snapshot primitives that enforce D-07 freeze timing, D-08 provider priority (`metar -> archive -> manual`), D-09 immutable base with append-only corrections, and D-10 non-blocking failure behavior with `weather_missing` and reason fields.</action>
  <verify>
    <automated>python -m pytest tests/test_weather_snapshot.py -q</automated>
  </verify>
  <acceptance_criteria>Resolver returns immutable baseline records with provenance metadata, appends correction records instead of mutating base payload, and emits deterministic failure markers without raising blocking import exceptions.</acceptance_criteria>
  <done>Weather correlation logic is encapsulated and independently testable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire snapshot capture at decision-time and import/finalize fallback</name>
  <files>custom_components/flynow/coordinator.py, custom_components/flynow/flight_log.py, tests/test_coordinator.py, tests/test_flight_log.py</files>
  <read_first>
    - custom_components/flynow/coordinator.py
    - custom_components/flynow/flight_log.py
    - .planning/phases/07-flight-log-import-map-visualization-backlog/07-01-PLAN.md
  </read_first>
  <action>Hook decision-time forecast freezing in coordinator path per D-07, then add fallback freeze in import/finalization service path for records without pre-existing snapshot. Keep service behavior non-blocking when provider lookups fail per D-10 and persist failure reason for later correlation UI.</action>
  <verify>
    <automated>python -m pytest tests/test_coordinator.py tests/test_flight_log.py -q</automated>
  </verify>
  <acceptance_criteria>Decision-ready windows create snapshot records, delayed imports backfill missing snapshots, and provider failures preserve successful import flow with explicit metadata.</acceptance_criteria>
  <done>Snapshot lifecycle is complete from decision-time through delayed-import fallback.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Expose compact correlation projection to sensor attributes</name>
  <files>custom_components/flynow/sensor.py, custom_components/flynow/flight_log.py, tests/test_sensor.py</files>
  <read_first>
    - custom_components/flynow/sensor.py
    - custom_components/flynow/flight_log.py
    - .planning/phases/07-flight-log-import-map-visualization-backlog/07-PATTERNS.md
  </read_first>
  <action>Project summary-only correlation fields (forecast snapshot summary, observed source, weather-missing flags, correction counts) into sensor attributes to keep payload bounded and frontend-ready; do not expose full raw track arrays in HA state.</action>
  <verify>
    <automated>python -m pytest tests/test_sensor.py -q</automated>
  </verify>
  <acceptance_criteria>Sensor attributes expose correlation-ready summary fields with stable keys while large raw sidecar payload remains off sensor state.</acceptance_criteria>
  <done>Frontend can consume correlation metadata without risking HA state bloat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FlyNow forecast engine -> immutable snapshot store | Internal decision data crosses into long-lived audit record |
| external weather APIs -> weather resolver | Untrusted provider responses enter correlation records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | T | `weather_snapshot.py` provider ingestion | mitigate | Persist provider name/station/timestamp and strict field normalization before storage |
| T-07-06 | R | correction history | mitigate | Append-only correction entries with `source`, `reason`, and timestamp metadata |
| T-07-07 | I | sensor projection | mitigate | Export only compact summary values; keep raw traces and full blobs in sidecar files |
| T-07-08 | D | provider timeout/failure path | mitigate | Timeout external lookups and set `weather_missing` markers while continuing import |
</threat_model>

<verification>
- `python -m pytest tests/test_weather_snapshot.py -q`
- `python -m pytest tests/test_coordinator.py tests/test_flight_log.py -q`
- `python -m pytest tests/test_sensor.py -q`
</verification>

<success_criteria>
- Forecast snapshots are frozen deterministically and never mutated in place.
- Fallback snapshot capture exists for delayed import/finalization when decision-time snapshot is absent.
- Observed weather source chain is deterministic and provenance-tagged.
- Weather lookup failures no longer block import completion and are explicitly represented.
- Sensor payload includes bounded correlation summaries required by frontend integration.
</success_criteria>

<output>
After completion, create `.planning/phases/07-flight-log-import-map-visualization-backlog/07-02-SUMMARY.md`
</output>
