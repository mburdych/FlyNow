---
phase: 07-flight-log-import-map-visualization-backlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/flynow/flight_import.py
  - custom_components/flynow/flight_sidecar_store.py
  - custom_components/flynow/flight_log.py
  - custom_components/flynow/const.py
  - tests/test_flight_import.py
  - tests/test_flight_sidecar_store.py
autonomous: true
requirements:
  - BL-07-01
  - BL-07-02
  - BL-07-06
must_haves:
  truths:
    - "User can import GPX and CSV flight tracks in one supported service flow."
    - "Import accepts timezone-safe timestamps only and rejects naive local timestamps."
    - "Invalid points are dropped while valid points remain persisted, with user-visible warnings."
    - "Imported track data is linked to a stable flight UUID without breaking existing flight log consumers."
  artifacts:
    - path: custom_components/flynow/flight_import.py
      provides: "Canonical GPX/CSV parser + validator + warning model"
    - path: custom_components/flynow/flight_sidecar_store.py
      provides: "Atomic sidecar persistence keyed by flight UUID"
    - path: custom_components/flynow/flight_log.py
      provides: "Import service registration and UUID linkage to legacy records"
  key_links:
    - from: custom_components/flynow/flight_import.py
      to: custom_components/flynow/flight_sidecar_store.py
      via: "validated payload upsert keyed by flight_id"
      pattern: "async_upsert|flight_id"
    - from: custom_components/flynow/flight_log.py
      to: custom_components/flynow/flight_sidecar_store.py
      via: "service handler linkage preserving legacy schema"
      pattern: "SERVICE_IMPORT|flight_id|schema_version"
---

<objective>
Deliver the import and persistence foundation for Phase 07 by implementing GPX/CSV parsing, validation, sidecar storage, and flight UUID linkage.

Purpose: Establish delayed-import-safe ingestion and storage contracts before weather correlation and map rendering build on top.
Output: New backend import modules, sidecar persistence, and tests validating parser behavior and backward compatibility.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-flight-log-import-map-visualization-backlog/07-CONTEXT.md
@.planning/phases/07-flight-log-import-map-visualization-backlog/07-RESEARCH.md
@.planning/phases/07-flight-log-import-map-visualization-backlog/07-PATTERNS.md
@custom_components/flynow/flight_log.py
@custom_components/flynow/coordinator.py
@tests/test_flight_log.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build canonical import parser and validation pipeline</name>
  <files>custom_components/flynow/flight_import.py, custom_components/flynow/const.py, tests/test_flight_import.py</files>
  <read_first>
    - .planning/phases/07-flight-log-import-map-visualization-backlog/07-CONTEXT.md
    - .planning/phases/07-flight-log-import-map-visualization-backlog/07-RESEARCH.md
    - custom_components/flynow/flight_log.py
  </read_first>
  <action>Implement GPX and CSV import parsing per D-01, enforce timezone policy per D-02 (UTC/RFC3339 or explicit offset only), and implement partial-success behavior per D-03 (drop invalid points, keep valid points, emit structured warnings). Use backend-canonical validation and async-safe contracts compatible with HA service handlers.</action>
  <verify>
    <automated>python -m pytest tests/test_flight_import.py -q</automated>
  </verify>
  <acceptance_criteria>Parser accepts both GPX and CSV; naive timestamps fail with explicit error; mixed-quality inputs return valid points plus warning list without aborting entire import.</acceptance_criteria>
  <done>Import module exposes deterministic normalized payload schema consumed by sidecar persistence and service layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sidecar persistence keyed by stable flight UUID</name>
  <files>custom_components/flynow/flight_sidecar_store.py, tests/test_flight_sidecar_store.py</files>
  <read_first>
    - .planning/phases/07-flight-log-import-map-visualization-backlog/07-CONTEXT.md
    - custom_components/flynow/flight_log.py
    - tests/test_flight_log.py
  </read_first>
  <action>Create an atomic JSON sidecar store per D-04 keyed by stable UUID from D-06, storing full raw points and derived summaries per D-05. Preserve corruption recovery and lock-guarded read-modify-write semantics matching existing `flight_log.py` patterns.</action>
  <verify>
    <automated>python -m pytest tests/test_flight_sidecar_store.py -q</automated>
  </verify>
  <acceptance_criteria>Sidecar record upsert is atomic, deterministic by `flight_id`, includes raw points and summary metrics, and does not alter existing `flynow_flights.json` record shape.</acceptance_criteria>
  <done>Sidecar storage contract is production-ready and independently test-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire import service into flight log domain without breaking compatibility</name>
  <files>custom_components/flynow/flight_log.py, custom_components/flynow/const.py, tests/test_flight_log.py</files>
  <read_first>
    - custom_components/flynow/flight_log.py
    - custom_components/flynow/const.py
    - .planning/phases/07-flight-log-import-map-visualization-backlog/07-PATTERNS.md
  </read_first>
  <action>Register import-related service handlers that invoke parser + sidecar store, attach/resolve stable flight UUID linkage per D-06, and preserve all existing `log_flight` and `list_flights` behavior for legacy consumers from Phase 03. Return warning payloads for dropped points and non-blocking enrichment status fields for downstream plans.</action>
  <verify>
    <automated>python -m pytest tests/test_flight_log.py -q</automated>
  </verify>
  <acceptance_criteria>New import service responds with `flight_id`, warning payload, and persisted sidecar linkage while pre-existing flight log tests continue passing.</acceptance_criteria>
  <done>Import foundation is callable from HA service boundary and backward compatibility is maintained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user upload -> HA import service | Untrusted GPX/CSV payload enters backend validation boundary |
| import parser -> local sidecar files | Parsed content crosses into persistent local storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T | `flight_import.py` CSV handling | mitigate | Validate headers, strict schema checks, and sanitize formula-like leading characters in exported warning echoes |
| T-07-02 | D | GPX/CSV parser path | mitigate | Enforce file/point count caps and bounded parse loops with explicit reject errors |
| T-07-03 | R | sidecar persistence lineage | mitigate | Persist immutable `flight_id`, `created_at`, and warning provenance for traceability |
| T-07-04 | I | sidecar raw track storage | mitigate | Keep raw track out of sensor attributes and expose summarized projection only |
</threat_model>

<verification>
- `python -m pytest tests/test_flight_import.py -q`
- `python -m pytest tests/test_flight_sidecar_store.py -q`
- `python -m pytest tests/test_flight_log.py -q`
</verification>

<success_criteria>
- GPX and CSV imports are accepted as first-class formats with deterministic validation.
- Timestamp normalization rejects naive local timestamps and retains timezone-safe values.
- Partial-success import behavior preserves valid points and reports dropped-point warnings.
- Sidecar persistence keyed by stable flight UUID stores raw points + derived summary metrics.
- Existing flight log consumers remain compatible with unchanged core record structure.
</success_criteria>

<output>
After completion, create `.planning/phases/07-flight-log-import-map-visualization-backlog/07-01-SUMMARY.md`
</output>
