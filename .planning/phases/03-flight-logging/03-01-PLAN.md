---
phase: 03-flight-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/flynow/const.py
  - custom_components/flynow/__init__.py
  - custom_components/flynow/flight_log.py
  - custom_components/flynow/services.yaml
  - tests/test_flight_log.py
autonomous: false
requirements:
  - LOG-02
  - LOG-01
must_haves:
  truths:
    - "Submitting a valid flight log persists one complete record to `/config/flynow_flights.json`."
    - "Interrupted or failed writes do not leave a partially written or corrupted JSON file."
    - "Backend rejects invalid payloads (missing required field, invalid outcome, invalid site) instead of storing bad data."
  artifacts:
    - path: custom_components/flynow/flight_log.py
      provides: "Service schema, normalization, list/append operations, and atomic persistence helpers"
    - path: custom_components/flynow/services.yaml
      provides: "HA service contract metadata for `flynow.log_flight` and `flynow.list_flights`"
    - path: tests/test_flight_log.py
      provides: "Automated regression tests for schema, atomic persistence path, and service responses"
  key_links:
    - from: custom_components/flynow/__init__.py
      to: custom_components/flynow/flight_log.py
      via: "integration setup calls `async_register_services(hass)`"
      pattern: "async_register_services"
    - from: custom_components/flynow/flight_log.py
      to: /config/flynow_flights.json
      via: "resolved with `hass.config.path(\"flynow_flights.json\")` and written atomically"
      pattern: "hass\\.config\\.path|atomic_writes"
    - from: custom_components/flynow/flight_log.py
      to: Home Assistant service bus
      via: "domain services return structured response payloads"
      pattern: "hass\\.services\\.async_register|SupportsResponse\\.ONLY"
---

<objective>
Implement the backend flight logging contract: service registration, payload validation/defaulting, and atomic JSON persistence at `/config/flynow_flights.json`.

Purpose: Deliver the storage and API foundation required by LOG-02 while preparing the exact service interface the card will call for LOG-01.
Output: New `flight_log.py` module, integration service wiring, service metadata, and automated backend tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-flight-logging/03-CONTEXT.md
@.planning/phases/03-flight-logging/03-RESEARCH.md
@.planning/phases/03-flight-logging/03-PATTERNS.md
@custom_components/flynow/__init__.py
@custom_components/flynow/const.py
@tests/test_coordinator.py

<interfaces>
From `custom_components/flynow/__init__.py`:
- `async_setup_entry(hass, entry) -> bool`
- `async_unload_entry(hass, entry) -> bool`

From `custom_components/flynow/const.py`:
- `DOMAIN = "flynow"`
- `SITE_IDS = ("lzmada", "katarinka", "nitra-luka")`
- existing selected-site constants already used by coordinator/sensor

Task output contract for later plans:
- `flynow.log_flight` service (`SupportsResponse.ONLY`) returns `{ "entry": <normalized_record> }`
- `flynow.list_flights` service (`SupportsResponse.ONLY`) returns `{ "flights": [<normalized_record>, ...] }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and test the flight-log service/data contract first</name>
  <read_first>
    - `.planning/phases/03-flight-logging/03-CONTEXT.md` (D-01, D-02, D-03, D-04)
    - `.planning/phases/03-flight-logging/03-RESEARCH.md` (service + atomic write recommendations)
    - `.planning/phases/03-flight-logging/03-PATTERNS.md` (backend analog: `custom_components/flynow/notifications.py`; constants analog: `custom_components/flynow/const.py`)
    - `custom_components/flynow/const.py` (reuse `SITE_IDS`, `DOMAIN`)
  </read_first>
  <files>tests/test_flight_log.py, custom_components/flynow/const.py, custom_components/flynow/services.yaml</files>
  <action>Create failing tests in `tests/test_flight_log.py` for: (a) required LOG-01 fields except optional notes per D-01, (b) default handling per D-02 (duration=90 when omitted from UI payload path), (c) outcome enum locked to `flown|cancelled-weather|cancelled-other` per D-03, (d) launch time stored in `HH:MM` 24-hour format per D-04, (e) malformed JSON recovery behavior (backup + reinitialize), (f) bounded history response (`HISTORY_LIMIT = 200`, newest-first), and (g) response payload shapes for `log_flight` and `list_flights`. Follow backend testing shape from `03-PATTERNS.md` analog `tests/test_coordinator.py` (lightweight fake hass/services). Add constants in `custom_components/flynow/const.py`: `FLIGHT_LOG_FILENAME = "flynow_flights.json"`, `SERVICE_LOG_FLIGHT = "log_flight"`, `SERVICE_LIST_FLIGHTS = "list_flights"`, `FLIGHT_OUTCOMES`, `BALLOON_IDS`, and `FLIGHT_HISTORY_LIMIT = 200`. Add `custom_components/flynow/services.yaml` metadata with explicit fields/selectors for both services using the same enum/value sets as tests.</action>
  <verify>
    <automated>pytest tests/test_flight_log.py -x</automated>
  </verify>
  <acceptance_criteria>Tests assert the exact field contract and constants/services metadata encode the same allowed values; initially failing tests identify missing runtime implementation.</acceptance_criteria>
  <done>The repository contains an explicit, test-backed backend contract for LOG-01/LOG-02 with locked decision IDs represented in assertions and metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement backend persistence and service handlers with atomic writes</name>
  <read_first>
    - `custom_components/flynow/__init__.py`
    - `custom_components/flynow/const.py`
    - `.planning/phases/03-flight-logging/03-PATTERNS.md` (service analog: `custom_components/flynow/notifications.py`; wiring analog: `custom_components/flynow/__init__.py`)
    - `tests/test_flight_log.py` (from Task 1)
  </read_first>
  <files>custom_components/flynow/flight_log.py, custom_components/flynow/__init__.py</files>
  <action>Create `custom_components/flynow/flight_log.py` with `LOG_FLIGHT_SCHEMA`, `LIST_FLIGHTS_SCHEMA`, `FlightLogStore`, and `async_register_services(hass)`, following service/result structuring style from `03-PATTERNS.md` analog `custom_components/flynow/notifications.py`. In `FlightLogStore`, resolve path with `hass.config.path(FLIGHT_LOG_FILENAME)`, use HA JSON helper atomic writes (`atomic_writes=True`) through `hass.async_add_executor_job`, guard append/list operations with `asyncio.Lock`, normalize stored records to include all LOG-01 fields plus server metadata (`id`, `created_at`, `schema_version`), and keep notes optional but always persisted as string. Implement malformed-file recovery per research resolution: on decode failure, move bad file to `flynow_flights.json.corrupt-<UTC-timestamp>`, log warning with backup path, and continue from empty list. Implement bounded list response per research resolution: `list_flights` returns newest-first entries limited to `FLIGHT_HISTORY_LIMIT` while preserving full on-disk history. In `custom_components/flynow/__init__.py`, call `await async_register_services(hass)` during setup with idempotent guard so services are registered once per domain. Ensure validation rejects unknown extra keys and invalid enum values before persistence.</action>
  <verify>
    <automated>pytest tests/test_flight_log.py -x</automated>
  </verify>
  <acceptance_criteria>`pytest` passes with coverage for append/list success path, invalid payload rejection, and atomic write path behavior; service registration is visible through hass.services mocks and no duplicate-registration failure occurs on repeated setup.</acceptance_criteria>
  <done>Backend supports production-ready `flynow.log_flight` and `flynow.list_flights` operations with atomic local JSON persistence and strict schema validation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify malformed-file recovery and history bound risk controls</name>
  <what-built>Backend now includes malformed JSON recovery with backup and bounded history responses to protect frontend/runtime behavior.</what-built>
  <action>Execute focused risk verification for malformed-file recovery and history bounds before approving Phase 03 backend completion.</action>
  <how-to-verify>
    1. Run `pytest tests/test_flight_log.py -x`.
    2. Confirm explicit assertions exist and pass for malformed-file recovery:
       - a malformed `flynow_flights.json` produces one `*.corrupt-<UTC-timestamp>` backup
       - `list_flights` still returns a valid response with an empty or recovered list after fallback
       - subsequent `log_flight` append succeeds without manual cleanup
    3. Confirm explicit assertions exist and pass for history bounds:
       - response count never exceeds `FLIGHT_HISTORY_LIMIT` (200)
       - ordering is newest-first in bounded output.
  </how-to-verify>
  <verify>
    <automated>pytest tests/test_flight_log.py -x -k "malformed or history_limit or list_flights"</automated>
  </verify>
  <done>Risk-control assertions for malformed-file recovery and bounded history all pass and no manual workaround is required to resume normal logging.</done>
  <resume-signal>Type "approved" if all assertions pass, or provide failing assertion names to revise.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lovelace card → HA service | Untrusted form payload crosses from browser context into backend persistence handler |
| Integration runtime → file system (`/config`) | Persistent storage boundary where corruption or tampering risks must be controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `custom_components/flynow/flight_log.py` service input | mitigate | Enforce `vol.Schema(..., extra=vol.PREVENT_EXTRA)` with enum/date/time/duration validation before write |
| T-03-02 | D | `notes` and list payload size | mitigate | Cap notes length in schema and return deterministic list payload structure |
| T-03-03 | T | `/config/flynow_flights.json` write path | mitigate | Use HA helper atomic writes via executor and lock append flow with `asyncio.Lock` |
| T-03-04 | R | service call traceability | accept | Keep server-assigned `id`/`created_at` metadata for each stored record |
</threat_model>

<verification>
- `pytest tests/test_flight_log.py -x`
- `pytest tests/test_coordinator.py -x` (regression check for setup/coordinator stability)
</verification>

<success_criteria>
- A valid service call stores one complete LOG-01 record at `/config/flynow_flights.json`.
- Invalid inputs never mutate persisted storage.
- Storage writes remain atomic and list retrieval returns normalized newest-first records.
</success_criteria>

<output>
After completion, create `.planning/phases/03-flight-logging/03-01-SUMMARY.md`
</output>
