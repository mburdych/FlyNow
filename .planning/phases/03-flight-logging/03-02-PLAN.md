---
phase: 03-flight-logging
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lovelace/flynow-card/src/types.ts
  - lovelace/flynow-card/src/flynow-card.ts
  - lovelace/flynow-card/src/flight-log-types.ts
  - tests/test_card_contract.py
autonomous: true
requirements:
  - LOG-01
must_haves:
  truths:
    - "Crew can open the card and submit a completed flight with all required fields and optional notes."
    - "Form defaults are prefilled exactly per D-02 for repeated operations."
    - "Crew can see previously logged flights from backend responses in the card history section."
  artifacts:
    - path: lovelace/flynow-card/src/flight-log-types.ts
      provides: "Typed form payload and response contracts"
    - path: lovelace/flynow-card/src/flynow-card.ts
      provides: "Flight log form UX, service calls, success/error feedback, history rendering"
    - path: tests/test_card_contract.py
      provides: "Regression checks for logging form labels, field contract markers, and history section"
  key_links:
    - from: lovelace/flynow-card/src/flynow-card.ts
      to: flynow.log_flight
      via: "hass.callService return-response call"
      pattern: "callService\\(\"flynow\",\\s*\"log_flight\""
    - from: lovelace/flynow-card/src/flynow-card.ts
      to: flynow.list_flights
      via: "history refresh call after mount/submit"
      pattern: "callService\\(\"flynow\",\\s*\"list_flights\""
    - from: lovelace/flynow-card/src/flynow-card.ts
      to: lovelace/flynow-card/src/flight-log-types.ts
      via: "typed payload/response interfaces"
      pattern: "LogFlight|LoggedFlight|FlightOutcome"
---

<objective>
Implement the Lovelace flight logging UX and typed service integration against the backend contract from Plan 03-01.

Purpose: Fulfill LOG-01 in the card with a fast, repeatable form workflow and visible history retrieval for previously logged flights.
Output: Card form + history UI, TypeScript service contracts, and contract tests validating the logging surface.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-flight-logging/03-CONTEXT.md
@.planning/phases/03-flight-logging/03-RESEARCH.md
@.planning/phases/03-flight-logging/03-PATTERNS.md
@.planning/phases/03-flight-logging/03-01-PLAN.md
@lovelace/flynow-card/src/flynow-card.ts
@lovelace/flynow-card/src/types.ts
@tests/test_card_contract.py

<interfaces>
From Wave 1 backend (`03-01` output contract):
- `flynow.log_flight` request includes: `date`, `balloon`, `launch_time`, `duration_min`, `site`, `outcome`, optional `notes`
- `flynow.log_flight` response includes `{ entry: LoggedFlight }`
- `flynow.list_flights` response includes `{ flights: LoggedFlight[] }`

From current frontend:
- `HomeAssistantLike` currently exposes `states`; this plan extends it with typed `callService`.
- Existing card already renders site summaries and selected-site details in `FlyNowCard`; new log section must preserve this behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed logging service contracts and failing card contract checks</name>
  <read_first>
    - `.planning/phases/03-flight-logging/03-CONTEXT.md` (D-01..D-04)
    - `.planning/phases/03-flight-logging/03-PATTERNS.md` (frontend analog: `lovelace/flynow-card/src/flynow-card.ts`; contract-test analog: `tests/test_card_contract.py`)
    - `lovelace/flynow-card/src/types.ts`
    - `tests/test_card_contract.py`
  </read_first>
  <files>lovelace/flynow-card/src/flight-log-types.ts, lovelace/flynow-card/src/types.ts, tests/test_card_contract.py</files>
  <action>Create `flight-log-types.ts` with `FlightOutcome` union (`flown | cancelled-weather | cancelled-other` per D-03), `LogFlightPayload`, `LoggedFlight`, and list/submit response interfaces, following type-export conventions from `03-PATTERNS.md` analog `lovelace/flynow-card/src/types.ts`. Extend `HomeAssistantLike` in `types.ts` with typed `callService<T>(..., returnResponse?: boolean)` signature. Add/extend failing contract tests in `tests/test_card_contract.py` (analog style from `03-PATTERNS.md`) that assert presence of required logging form field labels/identifiers (D-01), 24h time picker marker (`type=\"time\"`, D-04), and history section text placeholders for previous logs including a bounded-list indicator that aligns with backend history limits.</action>
  <verify>
    <automated>pytest tests/test_card_contract.py -x</automated>
  </verify>
  <acceptance_criteria>New/updated contract tests fail until the card implements the flight logging section, and type files compile with no unresolved imports in the card source.</acceptance_criteria>
  <done>Frontend has a strict TS contract for both service calls and card tests now enforce required logging UI/contract markers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement card flight-log form defaults, submit flow, and history rendering</name>
  <read_first>
    - `.planning/phases/03-flight-logging/03-PATTERNS.md` (render/event analogs in `lovelace/flynow-card/src/flynow-card.ts`)
    - `lovelace/flynow-card/src/flynow-card.ts`
    - `lovelace/flynow-card/src/flight-log-types.ts`
    - `custom_components/flynow/services.yaml`
  </read_first>
  <files>lovelace/flynow-card/src/flynow-card.ts</files>
  <action>In `FlyNowCard`, add reactive state for log form values, submit status, and `flights` history, following existing render decomposition/event-handling patterns from `03-PATTERNS.md` analog `lovelace/flynow-card/src/flynow-card.ts`. Implement form fields for date/balloon/launch time/duration/site/outcome/notes with required markers matching D-01; keep notes optional. Apply D-02 defaults exactly: date=today local, balloon=last used (persisted in localStorage key), site=current selected site ID, duration=90. Use `<input type="time">` for launch time (D-04). On submit, call `this.hass.callService(\"flynow\", \"log_flight\", payload, undefined, false, true)` and then refresh history with `list_flights`; render only backend-returned bounded entries and show clear success/error message while preserving current summary/detail sections from Phase 4. Keep outcomes fixed to D-03 enum values and do not introduce edit/delete behaviors in this phase.</action>
  <verify>
    <automated>npm --prefix lovelace/flynow-card run build && pytest tests/test_card_contract.py -x</automated>
  </verify>
  <acceptance_criteria>Build succeeds, contract tests pass, and card source contains deterministic logging section with required fields, defaults, enum options, 24-hour time input, service calls, and previous-log list rendering.</acceptance_criteria>
  <done>Card supports end-to-end log creation and history display via backend services without regressing existing multi-site forecast sections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → card state | Browser-entered values may be malformed or incomplete before backend validation |
| Card service call → backend response | Card must handle service failures safely and avoid silent data loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | T | `flynow-card.ts` form payload | mitigate | Restrict UI options to fixed enums/site IDs and required controls; rely on backend schema as final gate |
| T-03-06 | D | repeated submit clicks | mitigate | Disable submit during in-flight request and show explicit status state |
| T-03-07 | I | stale or missing history view | mitigate | Always call `list_flights` after successful submit and on initial load |
| T-03-08 | R | ambiguous user feedback | mitigate | Render explicit success/error microcopy for each submit attempt |
</threat_model>

<verification>
- `npm --prefix lovelace/flynow-card run build`
- `pytest tests/test_card_contract.py -x`
</verification>

<success_criteria>
- Card exposes a complete flight log form with locked defaults and enum/time constraints.
- Submitting the form uses backend services and updates visible history in-card.
- Existing comparison-first forecast card sections remain intact after logging UI addition.
</success_criteria>

<output>
After completion, create `.planning/phases/03-flight-logging/03-02-SUMMARY.md`
</output>
