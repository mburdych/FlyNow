# Phase 999.1: Lovelace Card Language Toggle (BACKLOG) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-28
**Phase:** 999.1-lovelace-card-language-toggle-backlog
**Areas discussed:** Language Selector UX, Persistence Policy, Translation Architecture

---

## Language Selector UX

| Option | Description | Selected |
|--------|-------------|----------|
| Header segmented control | Top-right in card header as SK/EN segmented control | ✓ |
| Settings row | In a small display/settings row near top details | |
| Footer link | Bottom compact text toggle | |

**User's choice:** Header segmented control.
**Notes:** Switching should be one-tap direct SK/EN toggle, and selector visibility should be in collapsible mode.

---

## Persistence Policy

| Option | Description | Selected |
|--------|-------------|----------|
| Frontend localStorage | Persist in card localStorage key | ✓ |
| HA user preference | Persist in Home Assistant user-level state | |
| Session only | Keep in memory until reload | |

**User's choice:** localStorage persistence.
**Notes:** On first load, detect from `hass.language` once and persist immediately. If stored value is invalid, re-detect and re-save valid value.

---

## Translation Architecture

| Option | Description | Selected |
|--------|-------------|----------|
| Single typed dictionary | Object keyed by language and key | ✓ |
| Separate files | Dedicated file per language | |
| Inline strings | Strings localized at each render site | |

**User's choice:** Single typed dictionary.
**Notes:** Enforce strict key parity between SK/EN and fall back to EN when a key is missing at runtime.

---

## Claude's Discretion

- Exact implementation details for collapsible settings UX (iconography, spacing, and animation).
- Internal naming for dictionary helpers and persistent storage key constants.

## Deferred Ideas

None.
