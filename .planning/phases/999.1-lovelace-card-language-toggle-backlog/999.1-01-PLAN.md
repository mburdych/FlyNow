---
phase: 999.1-lovelace-card-language-toggle-backlog
plan: 01
type: execute
wave: 1
depends_on:
  - 05-translations-fix/05-01
files_modified:
  - lovelace/flynow-card/src/flynow-card.ts
  - lovelace/flynow-card/src/types.ts
autonomous: true
requirements:
  - BL-999.1-01
  - BL-999.1-02
  - BL-999.1-03
  - BL-999.1-04
  - BL-999.1-05
must_haves:
  truths:
    - "Card exposes an explicit SK/EN segmented control in a collapsible settings area in the header region."
    - "Language is initialized from localStorage first; hass.language is used only when value is absent or invalid, then persisted."
    - "All card UI labels/messages are served through one typed SK/EN dictionary with strict key parity."
    - "Missing runtime translation key falls back to English without breaking rendering."
    - "Language switch applies immediately across all card sections and remains stable after dashboard reload."
  artifacts:
    - path: lovelace/flynow-card/src/flynow-card.ts
      provides: "Language state lifecycle, selector UI wiring, and translated rendering"
    - path: lovelace/flynow-card/src/types.ts
      provides: "Shared language/translation contracts reused by card implementation"
  key_links:
    - from: lovelace/flynow-card/src/flynow-card.ts
      to: Home Assistant frontend locale context
      via: "First-load fallback from hass.language when persisted value is absent/invalid"
      pattern: "hass\\.language|localStorage"
    - from: lovelace/flynow-card/src/flynow-card.ts
      to: lovelace/flynow-card/src/types.ts
      via: "Typed dictionary and key parity contracts for SK/EN strings"
      pattern: "Record<|TranslationKey|Language"
    - from: lovelace/flynow-card/src/flynow-card.ts
      to: all card section render helpers
      via: "Translator helper drives visible labels/messages consistently"
      pattern: "t\\(|labels|render"
---

<objective>
Implement a deterministic, user-controlled language toggle for the FlyNow Lovelace card that supports Slovak and English, persists preference in frontend storage, and updates all card text immediately.

Purpose: Resolve post-v1.1 UX feedback by making language behavior explicit and predictable instead of relying on automatic locale inference every render.

Output: Updated card frontend code with (1) collapsible SK/EN selector, (2) persisted selected language state, (3) typed translation dictionary with fallback behavior, and (4) verification evidence for both SK and EN profiles.
</objective>

<execution_context>
@.planning/phases/999.1-lovelace-card-language-toggle-backlog/999.1-CONTEXT.md
@.planning/phases/999.1-lovelace-card-language-toggle-backlog/999.1-RESEARCH.md
@.planning/phases/999.1-lovelace-card-language-toggle-backlog/999.1-UI-SPEC.md
@.planning/ROADMAP.md
</execution_context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add typed SK/EN dictionary contracts and fallback translator</name>
  <files>lovelace/flynow-card/src/types.ts, lovelace/flynow-card/src/flynow-card.ts</files>
  <action>Introduce explicit language and translation key contracts, define a single runtime dictionary covering both languages with strict key parity, and add a translator helper that falls back to English for missing selected-language keys.</action>
  <verify>
    <automated>npm --prefix lovelace/flynow-card run typecheck</automated>
  </verify>
  <done>Card compiles with typed dictionary parity and translator helper is available for downstream render paths.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement deterministic persisted language lifecycle</name>
  <files>lovelace/flynow-card/src/flynow-card.ts</files>
  <action>Add dedicated storage key and initialize reactive language state via read-validate-fallback-persist flow (`localStorage` first, `hass.language` only when absent/invalid). Ensure invalid stored values are corrected and persisted immediately.</action>
  <verify>
    <automated>python -c "from pathlib import Path; s=Path('lovelace/flynow-card/src/flynow-card.ts').read_text(encoding='utf-8'); assert 'localStorage' in s and 'hass.language' in s; print('init flow hooks present')"</automated>
  </verify>
  <done>Language choice is deterministic across refreshes and no longer re-derived on every render from HA/browser locale.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add collapsible settings selector with explicit SK/EN toggle</name>
  <files>lovelace/flynow-card/src/flynow-card.ts</files>
  <action>Implement header-level collapsible settings affordance (default collapsed) and render explicit segmented SK/EN selector inside it. Wire one-tap toggle handlers, persist immediately, and keep keyboard operability (Tab focus + Enter/Space activation).</action>
  <verify>
    <automated>npm --prefix lovelace/flynow-card run build</automated>
  </verify>
  <done>Card presents a discoverable but non-intrusive language selector that updates language state in one interaction.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Wire translator into all card-authored labels/messages and run UAT</name>
  <files>lovelace/flynow-card/src/flynow-card.ts</files>
  <action>Route all static card UI text and card-generated messages through translator helper across status, summary, controls, logging, and history sections. Keep backend-originated dynamic values unchanged unless already dictionary-backed.</action>
  <verify>
    <automated>npm --prefix lovelace/flynow-card run typecheck && npm --prefix lovelace/flynow-card run build</automated>
    <manual>Clear stored language key, verify first-load fallback from HA locale, switch SK/EN and confirm immediate full-card updates, reload to confirm persistence, then change HA profile locale and confirm explicit selection is still honored.</manual>
  </verify>
  <done>All in-scope card-authored labels/messages reflect selected language consistently with persistence guarantees.</done>
</task>

</tasks>

<verification>
- `npm --prefix lovelace/flynow-card run typecheck`
- `npm --prefix lovelace/flynow-card run build`
- `python -c "from pathlib import Path; p=Path('lovelace/flynow-card/src/flynow-card.ts'); s=p.read_text(encoding='utf-8'); assert 'localStorage' in s and 'hass.language' in s; print('deterministic init flow present')"`
- `python -c "from pathlib import Path; s=Path('lovelace/flynow-card/src/flynow-card.ts').read_text(encoding='utf-8'); assert 'SK' in s and 'EN' in s; print('explicit toggle labels present')"`
- Manual HA UAT:
  1. Clear browser storage key for card language, load dashboard, confirm initial language follows `hass.language`.
  2. Switch language via segmented control and confirm all visible card labels/messages update immediately.
  3. Reload dashboard and confirm selected language persists.
  4. Change HA profile/browser locale and confirm explicit prior selection remains honored.
</verification>

<success_criteria>
- Card contains an explicit SK/EN segmented language selector within a collapsible settings panel in header area.
- On first load without saved preference, language is inferred once from `hass.language`, persisted, and reused on subsequent renders.
- Invalid saved language values are auto-corrected to valid `sk`/`en` and rewritten.
- All user-facing card labels/messages are dictionary-backed with strict SK/EN key parity.
- Runtime missing key resolution falls back to English per key without rendering failure.
- Manual verification in HA confirms immediate toggle behavior and persistence independent of later HA/browser locale changes.
</success_criteria>
