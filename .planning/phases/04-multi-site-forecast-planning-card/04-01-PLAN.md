---
phase: 04-multi-site-forecast-planning-card
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/flynow/const.py
  - custom_components/flynow/coordinator.py
  - custom_components/flynow/sensor.py
  - lovelace/flynow-card/src/flynow-card.ts
  - tests/test_coordinator.py
  - tests/test_sensor.py
  - tests/test_card_contract.py
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04
must_haves:
  truths:
    - "User can see GO/NO-GO planning status for all three launch sites in one card view."
    - "Existing automations based on `binary_sensor.flynow_status` continue to work without reconfiguration."
    - "Each site summary includes launch window timing and fresh-enough update data."
  artifacts:
    - path: custom_components/flynow/const.py
      provides: "Static three-site catalog for multi-site planning"
    - path: custom_components/flynow/coordinator.py
      provides: "Per-site forecast fetch/analyze projection and summary payload"
    - path: custom_components/flynow/sensor.py
      provides: "Backward-compatible selected-site attributes plus sites summary"
    - path: lovelace/flynow-card/src/flynow-card.ts
      provides: "Comparison-first three-site planning UI"
  key_links:
    - from: custom_components/flynow/coordinator.py
      to: custom_components/flynow/open_meteo.py
      via: "fetch_forecast per static site coordinates"
      pattern: "fetch_forecast\\("
    - from: custom_components/flynow/sensor.py
      to: coordinator multi-site payload
      via: "selected site projection and sites_summary passthrough"
      pattern: "selected_site_id|sites_summary"
    - from: lovelace/flynow-card/src/flynow-card.ts
      to: sensor attributes
      via: "comparison render from sites_summary contract"
      pattern: "sites_summary"
---

<objective>
Deliver multi-site forecast planning in one Lovelace card by extending the current single-site integration to project three fixed launch sites from the launch-sites reference, while preserving existing binary sensor compatibility.

Purpose: Support practical flight planning across Malý Madaras, Katarínka, and Nitra lúka without adding new weather providers or breaking existing automations.
Output: Static multi-site metadata, multi-site coordinator projection, backward-compatible sensor attributes, and comparison-first card rendering with tests.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/reference/launch-sites.md
@.planning/phases/04-multi-site-forecast-planning-card/04-CONTEXT.md
@.planning/phases/04-multi-site-forecast-planning-card/04-RESEARCH.md
@custom_components/flynow/const.py
@custom_components/flynow/coordinator.py
@custom_components/flynow/sensor.py
@lovelace/flynow-card/src/flynow-card.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add locked multi-site catalog and coordinator per-site projection</name>
  <files>custom_components/flynow/const.py, custom_components/flynow/coordinator.py, tests/test_coordinator.py</files>
  <action>Implement static site catalog constants for exactly three sites from `.planning/reference/launch-sites.md` (D-01, D-02, D-08). Update coordinator refresh to iterate through sites, call `fetch_forecast` by each site `lat,lon` (D-03), analyze windows using existing analyzer/windows helpers, and build `sites`, `sites_summary`, and `selected_site_id` payload keys (D-04, D-06). Keep selected site default deterministic (`lzmada`) unless explicit config override exists.</action>
  <verify>
    <automated>pytest tests/test_coordinator.py -x</automated>
  </verify>
  <done>Coordinator returns deterministic multi-site payload with exactly three known site IDs and no new weather-source dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve legacy binary sensor behavior while exposing multi-site summary</name>
  <files>custom_components/flynow/sensor.py, tests/test_sensor.py</files>
  <action>Project selected-site data to existing top-level attributes (`active_window`, `launch_start`, `launch_end`, per-window keys) and keep `is_on` semantics unchanged for backward compatibility (D-05). Add explicit `selected_site_id` and `sites_summary` attributes for card consumption (D-06). Validate both old and new contracts with tests.</action>
  <verify>
    <automated>pytest tests/test_sensor.py -x</automated>
  </verify>
  <done>Legacy automation-facing keys still exist and new multi-site summary keys are available in one sensor entity payload.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement comparison-first multi-site planning card contract</name>
  <files>lovelace/flynow-card/src/flynow-card.ts, tests/test_card_contract.py</files>
  <action>Update card rendering to show a top comparison section for all three sites in fixed order (D-07, D-08), including GO/NO-GO and launch timing from `sites_summary`. Keep selected-site detail block below comparison and allow manual detail switch without hiding the comparison area (D-09). Extend card contract tests to assert presence of all three site labels and summary keys.</action>
  <verify>
    <automated>npm --prefix lovelace/flynow-card run build && pytest tests/test_card_contract.py -x</automated>
  </verify>
  <done>Card reliably shows all three site planning statuses first and still supports selected-site detailed condition view.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_coordinator.py tests/test_sensor.py tests/test_card_contract.py -x`
- `ruff check custom_components/flynow tests`
- `npm --prefix lovelace/flynow-card run build`
</verification>

<out_of_scope>
- Editing site catalog from config flow/UI.
- Adding METAR, SHMÚ, or any non-Open-Meteo data source.
- Notification strategy changes (dedup/fan-out/site-targeting).
- Historical learning model adjustments from flight logs.
</out_of_scope>

<success_criteria>
- Three fixed launch sites are available in integration state and rendered in comparison-first card UI.
- Existing `binary_sensor.flynow_status` automation behavior remains backward-compatible.
- Automated tests validate multi-site backend payload, sensor projection, and card contract.
</success_criteria>
